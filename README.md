# Redis 服务管理器

一个基于 Qt 6 的跨平台 Redis 服务管理工具，支持 Windows 和 Linux 系统。

<img width="1920" height="1080" alt="image" src="https://github.com/user-attachments/assets/d76e6b06-6f2d-4a6f-9400-46d12e620495" />


## 功能特性

- ✅ **自动下载安装** - 自动下载并安装 Redis（Windows 预编译版 / Linux 源码编译）
- ✅ **服务管理** - 启动、停止 Redis 服务
- ✅ **实时监控** - 自动检测并显示服务运行状态
- ✅ **配置管理** - 图形化配置 IP、端口和密码
- ✅ **端口检测** - 自动检查端口占用并显示占用进程
- ✅ **密码保护** - 支持设置 Redis 访问密码
- ✅ **现代化界面** - 扁平化设计，简洁美观
- ✅ **跨平台支持** - 完全兼容 Windows 和 Linux

## 系统要求

### Windows
- Windows 10/11
- Qt 6.5+
- CMake 3.19+
- Visual Studio 2019+ 或 MinGW

### Linux
- Ubuntu 20.04+ / Debian 11+ / Fedora 35+ / Arch Linux
- Qt 6.5+
- CMake 3.19+
- GCC/G++
- build-essential

## 快速开始

### Windows

#### 方法 1: 使用预编译版本
1. 下载最新的 Release 版本
2. 解压到任意目录
3. 运行 `RedisInstall.exe`

#### 方法 2: 从源码编译
```powershell
# 使用自动部署脚本
.\deploy_windows.bat

# 或手动编译
mkdir build
cd build
cmake ..
cmake --build . --config Release

# 部署依赖
cd ..
.\quick_deploy.bat
```

### Linux

#### 安装依赖
```bash
# 一键安装所有依赖
chmod +x install_dependencies_linux.sh
./install_dependencies_linux.sh
```

#### 编译项目
```bash
# 使用自动编译脚本
chmod +x build_linux.sh
./build_linux.sh

# 运行程序
cd RedisInstall-Linux
./start.sh
```

## 使用说明

### 启动程序

程序启动后会自动最大化显示主窗口，并立即检查 Redis 服务运行状态。

### 安装 Redis

如果 Redis 尚未安装：

1. 点击 **"📥 下载并安装 Redis"** 按钮
2. 程序会自动：
   - Windows: 下载 Redis 5.0.14.1 预编译版本
   - Linux: 下载 Redis stable 源码并自动编译
3. 显示下载进度和安装状态
4. 安装完成后即可启动服务

### 配置 Redis

**IP 地址**：默认 `0.0.0.0`（监听所有网络接口）

**端口**：默认 `10833`
- 输入端口时会自动检查是否被占用
- 如果端口被占用，会显示占用进程的名称和 PID

**密码**：可选
- 留空表示不设置密码
- 设置密码可提高安全性

修改配置后点击 **"应用更改"** 按钮保存。

### 服务管理

**启动服务**：点击 **"▶ 启动服务"** 按钮

**停止服务**：点击 **"⏸ 停止服务"** 按钮

**卸载**：点击 **"🗑 卸载"** 按钮可完全卸载 Redis

**状态监控**：
- 🟢 运行中
- 🔴 已停止
- 每 2 秒自动刷新状态

## 项目结构

```
RedisInstall/
├── main.cpp                          # 程序入口
├── mainwindow.cpp/h/ui               # 主窗口（最大化显示）
├── serviceconfig.cpp/h               # 配置管理
├── servicemanager.cpp/h              # 跨平台服务管理
├── portchecker.cpp/h                 # 端口检查及进程检测
├── redismanager.cpp/h                # Redis 下载、安装和运行管理
├── CMakeLists.txt                    # CMake 构建配置
├── build_linux.sh                    # Linux 编译脚本
├── install_dependencies_linux.sh     # Linux 依赖安装脚本
├── deploy_windows.bat                # Windows 完整部署脚本
├── quick_deploy.bat                  # Windows 快速部署脚本
└── USAGE.txt                         # 详细使用说明
```

## 技术栈

- **开发语言**：C++17
- **UI 框架**：Qt 6.5+ (Widgets)
- **构建系统**：CMake 3.19+
- **网络库**：Qt Network
- **跨平台**：
  - Windows: Service API, PowerShell, GetExtendedTcpTable
  - Linux: systemd, tar/make, lsof/ps

## 配置文件位置

- **Windows**: `%APPDATA%\RedisInstall\config.ini`
- **Linux**: `~/.config/RedisInstall/config.ini`

## Redis 安装位置

- **Windows**: `%LOCALAPPDATA%\RedisInstall\Redis`
- **Linux**: `~/.local/share/RedisInstall/redis`

## 故障排查

### Windows

**问题：提示缺少 DLL 文件**
```powershell
# 运行部署脚本
.\quick_deploy.bat
```

**问题：Redis 启动失败**
- 检查端口是否被占用
- 以管理员身份运行程序
- 查看 Redis 日志：`%LOCALAPPDATA%\RedisInstall\Redis\redis.log`

### Linux

**问题：编译失败**
```bash
# 重新安装依赖
./install_dependencies_linux.sh
```

**问题：Redis 启动失败 - Permission denied**
```bash
# 设置可执行权限
chmod +x ~/.local/share/RedisInstall/redis/redis-server
```

**问题：端口检查失败**
```bash
# 安装 lsof
sudo apt install lsof  # Ubuntu/Debian
sudo dnf install lsof  # Fedora
```

### 查看调试日志

从终端运行程序可以看到详细的调试输出：

```bash
# Windows (PowerShell)
.\RedisInstall.exe

# Linux
./RedisInstall
```

日志包括：
- `[MainWindow]` - 主窗口操作
- `[RedisManager]` - Redis 管理操作
- `[Redis Output]` - Redis 进程输出
- `[Redis Error]` - Redis 错误信息

## 开发指南

### 编译要求

- Qt 6.5 或更高版本
- CMake 3.19+
- C++17 编译器

### 在 Qt Creator 中打开

1. 打开 Qt Creator
2. 文件 → 打开文件或项目
3. 选择 `CMakeLists.txt`
4. 配置项目（选择 Qt 6.5+ kit）
5. 构建并运行

### 代码规范

- 界面语言：简体中文
- 代码风格：遵循 Qt 代码规范
- 跨平台：使用 `#ifdef Q_OS_WIN` 进行条件编译

## 许可证

本项目遵循 MIT 许可证。

## 贡献

欢迎提交 Issue 和 Pull Request！

## 更新日志

### v1.0.0 (2025-12-05)
- ✅ 初始版本发布
- ✅ 支持 Windows 和 Linux
- ✅ 自动下载安装 Redis
- ✅ 图形化配置管理
- ✅ 实时状态监控
- ✅ 端口占用检测
- ✅ 密码保护功能
- ✅ 现代化扁平界面

## 联系方式

如有问题或建议，请提交 Issue。

---

**Made with ❤️ using Qt 6**
